// import type { LayoutLoad } from './$types.d.ts';
// import { debugBase } from '$lib/ccg/ccg.svelte.ts';
// const debug = debugBase.extend('+layout.ts');

// export const load: LayoutLoad = async ({ url }) => {
//     debug(`Loading layout for ${url.pathname}`);
//     debug(`Full URL: ${url.href}`);
// };